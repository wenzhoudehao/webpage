# 进度记录：收款核销功能开发

> 更新时间：2026-03-02

---

## 已完成 ✅

### 1. Airtable 集成模块

**文件位置**：`libs/airtable/`

| 文件 | 说明 |
|------|------|
| `index.ts` | 主入口，导出所有公共 API |
| `types.ts` | 类型定义 |
| `client.ts` | 客户端实现（支持多 Base） |
| `utils.ts` | 工具函数（过滤公式、分页收集） |
| `README.md` | 模块使用文档 |

**功能特性**：
- ✅ 多 Base 支持（订单/样品/客户）
- ✅ View 过滤控制同步
- ✅ CRUD 操作（增删改查）
- ✅ 批量操作（最多 10 条）
- ✅ 自动分页收集

### 2. 配置文件

| 文件 | 说明 |
|------|------|
| `config/airtable.ts` | Airtable 配置（多 Base ID 映射） |
| `.env` | 环境变量（已填入 API Key 和 3 个 Base ID） |

### 3. 文档

| 文档 | 说明 |
|------|------|
| `docs/setup/02-Airtable连接配置.md` | Airtable 配置指南 |
| `docs/README.md` | 文档索引（已更新） |

---

## 环境配置

```env
AIRTABLE_API_KEY="patUCW8f8CkGNDR5l..."
AIRTABLE_BASE_ORDER="appCkvqCpKZR66zLL"    # 订单 Base
AIRTABLE_BASE_SAMPLE="appoCM9JVftYuVhuS"   # 样品 Base
AIRTABLE_BASE_CUSTOMER="appozeqJca7xzB7L0" # 客户 Base
```

---

## 待完成 📋

### 明天继续

1. **设计 Supabase 缓存表结构**
   - 收款核销缓存表
   - 同步状态字段设计

2. **实现数据同步逻辑**
   - Airtable → Supabase 定时同步
   - 基于 View 过滤增量同步
   - 同步状态回写 Airtable

3. **开发收款核销页面**
   - 订单列表展示
   - 核销操作界面
   - 状态更新

---

## 使用示例

```typescript
import { createAirtableClient, collectAll } from '@libs/airtable';

// 订单 Base
const poTable = createAirtableClient('PO表', { base: 'order' });

// 通过 View 获取待同步数据
const { records } = await poTable.list({ view: '待同步' });

// 收集所有分页数据
const allRecords = await collectAll(poTable, { view: '待核销' });
```

---

## 技术架构

```
┌─────────────┐     View 过滤      ┌─────────────┐
│  Airtable   │ ──────────────────►│  Supabase   │
│  (源数据)    │                    │  (缓存)     │
└─────────────┘                    └─────────────┘
       ▲                                  │
       │                                  │
       │         实时写入                  ▼
       └────────────────────────  ┌─────────────┐
                                  │   应用页面   │
                                  │  (收款核销)  │
                                  └─────────────┘
```

---

*记录时间：2026-03-02 晚*
